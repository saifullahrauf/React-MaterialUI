import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  table: {
    width: "100%",
    minWidth: "1000px",
    minHeight: "40vh",
    maxHeight: "40vh",
  },
});

export default useStyles;
